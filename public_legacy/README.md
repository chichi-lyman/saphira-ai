# Saphira AI™ Legacy Web Surface

The previous static dashboard frontend has been retired from the production deployment.

It exposed command-center, agent-network, mission-dashboard, and memory-vault surfaces that are intentionally replaced by the canonical `saphira-app` single-conversation experience.

The legacy source remains available in Git history for recovery/reference. It is not part of the Vercel build output.
