# This file was previously committed by mistake.
# It has been cleared. Real secrets must never be committed.
# Use .env.example as the template and keep local .env out of git (.gitignore already excludes it).
