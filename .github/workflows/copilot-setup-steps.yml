name: Copilot Setup Steps (Disabled)

# This file previously contained a malformed partial step that caused
# repeated workflow failures. It is kept as a valid no-op so that any
# remaining references do not break the Actions UI.
#
# Real setup lives in the other CI workflows (saphira-ci.yml, etc.).

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "copilot-setup-steps workflow is intentionally disabled. See other CI workflows."
